metastack3.1版本发布说明

重要提示：
metastack 是基于 Slurm 的功能增强和性能优化的版本，metastack3.1基于Slurm-24.05.8版本进行开发。

如果使用 slurmdbd（Slurm 数据库守护进程），则必须先更新此版本。

注意：如果使用主备DBD，则必须首先启动主数据库才能进行任何数据库转换。

metastack3.1的slurmdbd将与metastack2.0及以上版本、Slurm-20.11及以上版本的Slurm守护程序兼容。不需同时更新所有集群，但是在更新其他使用slurmdbd的集群之前，需首先更新slurmdbd并使其运行。

metastack3.1可以从metastack2.0及以上版本、Slurm-20.11或Slurm-21.08版本直接升级到该版本，不会丢失作业或其他状态信息。直接从早期版本的 Slurm 升级将导致状态信息的丢失。

从其他版本升级到metastack3.1时，所有SPANK插件必须重新编译。


要点
=====================================================================
-- 支持根据队列作业排队时间或排队个数节点借用
-- 支持基于消息队列的作业并行提交
-- 支持基于消息队列的作业epilog并行处理
-- 支持内存池配置
-- 支持基于线程池的agent机制性能优化
-- 支持低版本兼容，兼容metastack低版本与slurm官方版本
-- 大幅度优化大量小作业场景下作业吞吐效率

配置文件更改（详见man page帮助手册）
=====================================================================
-- slurm.conf
    - SlurmctldParameters新增配置子项rpc_queue_pool_size,para_submit,para_epilog,agent_thread_pool_size,no_step_complete_throttle,ignore_srun_job_complete,buf_pool_size=,job_pool_size